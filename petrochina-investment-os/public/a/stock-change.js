(()=>{async function updateStockChange(){try{const q=await fetch('/api/quotes',{cache:'no-store'}).then(r=>r.json());const s=q.stock;if(!s?.ok||typeof s.price!=='number'||typeof s.previousClose!=='number')return;const ch=s.price-s.previousClose;const pct=s.previousClose?ch/s.previousClose*100:0;const pctSign=pct>0?'+':pct<0?'-':'';const el=document.querySelector('#stockMeta');if(el)el.textContent=`前收 ${s.previousClose.toFixed(2)} ${pctSign}${Math.abs(pct).toFixed(2)}%`;}catch{}}setTimeout(updateStockChange,500);setInterval(updateStockChange,300000);})();