(()=>{const el=document.querySelector('#scenarioValue');if(!el)return;let applying=false;const round=()=>{if(applying)return;const v=Number(el.textContent);if(!Number.isFinite(v))return;const next=String(Math.round(v));if(el.textContent===next)return;applying=true;el.textContent=next;applying=false;};round();new MutationObserver(round).observe(el,{childList:true,characterData:true,subtree:true});})();