async function refreshBrentPreviousClose(){try{const q=await fetch('/api/quotes',{cache:'no-store'}).then(r=>r.json());const el=document.querySelector('#brentMeta');if(!el)return;if(q.brent?.ok){const pc=q.brent.previousClose;el.textContent=Number.isFinite(pc)?`前收 ${pc.toFixed(2)}`:'前收 —';}else{el.textContent='報價暫不可用';}}catch{}}refreshBrentPreviousClose();setInterval(refreshBrentPreviousClose,300000);